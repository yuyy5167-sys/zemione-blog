{
  "name": "site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/deployment-safety.mjs gate && astro build",
    "check:deploy-safety": "node --test scripts/deployment-safety.test.mjs",
    "check:production-contract": "node scripts/deployment-safety.mjs gate --strict",
    "deploy:workers-build": "node scripts/deployment-safety.mjs upload",
    "verify:release": "node scripts/deployment-safety.mjs verify",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^7.2.4"
  }
}
